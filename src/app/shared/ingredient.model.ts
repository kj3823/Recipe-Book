export class Ingredient{
  // public name:String;
  //
  // public amount:number;

  // constructor(name:String, amount:number) {
  //   this.name = name;
  //   this.amount = amount;
  // }

  // Shortcut which does the exact same thing only difference is that an accessory is added in front of the parameters.
  constructor(public name:String, public amount:number) {
  }
}
